import Reveal from '@/components/motion/Reveal'

export default function Telemetry() {
  return (
    <Reveal className="shell telemetry-statement">
      <p className="telemetry-body">
        One platform that discovers every AI model, maps its risk, enforces your
        policy, and produces audit evidence — continuously.
      </p>
    </Reveal>
  )
}
